{
  "name": "@dnb-hugo/blocks-bootstrap5",
  "description": "Bootstrap5-Blocks for Hugo",
  "version": "1.0.9",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "bugs": {
    "url": "https://github.com/dnb-hugo/blocks/issues"
  },
  "devDependencies": {
    "husky": "4.3.8",
    "standard-version": "9.1.0",
    "@dnb-hugo/commitlint-config": "github:dnb-hugo/commitlint-config",
    "@dnb-hugo/standard-version-config": "github:dnb-hugo/standard-version-config"
  },
  "homepage": "https://github.com/dnb-hugo/blocks/tree/master/bootstrap5",
  "license": "MIT",
  "private": true,
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"bootstrap5/v\" --releaseCommitMessageFormat \"chore(release): bootstrap5/v{{currentTag}}\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"bootstrap5/v\" --releaseCommitMessageFormat \"chore(release): bootstrap5/v{{currentTag}}\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"bootstrap5/v\" --releaseCommitMessageFormat \"chore(release): bootstrap5/v{{currentTag}}\" && git push --follow-tags origin main"
  },
  "commitlint": {
    "extends": [
      "@dnb-hugo/commitlint-config"
    ]
  }
}
