{
  "name": "@dnb-hugo/blocks",
  "version": "1.0.1",
  "description": "A collection of reusable page elements like headers, footers, content display etc. for Hugo.",
  "main": "index.js",
  "scripts": {
    "release": "standard-version --release-as patch -a && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a && git push --follow-tags origin main"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dnb-hugo/blocks.git"
  },
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dnb-hugo/blocks/issues"
  },
  "homepage": "https://github.com/dnb-hugo/blocks#readme",
  "devDependencies": {
    "commitizen": "4.2.2",
    "cz-customizable": "6.3.0",
    "cz-customizable-ghooks": "2.0.0",
    "@commitlint/cli": "11.0.0",
    "@commitlint/config-conventional": "11.0.0",
    "remark-preset-lint-dnb": "github:dnb-hugo/remark-preset-lint-dnb",
    "husky": "4.3.6"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-customizable"
    },
    "cz-customizable": {
      "config": ".cz_config.js"
    }
  },
  "private": true
}
